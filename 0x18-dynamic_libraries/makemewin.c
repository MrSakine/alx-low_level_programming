#include <stdio.h>

/**
 * puts - prints a string,
 * followed by a new line, to stdout
 *
 * @s: string to write
 *
 * Return: number of characters printed
 */
int puts(const char *s)
{
	return (printf("--> Please make me win!\n"));
}
